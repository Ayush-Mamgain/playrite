import React from 'react';

const TransactionModal = ({ walletAmount, setShowTransaction }) => {
    return (
        <div className='transactionModal'>
            Transaction modal
        </div>
    );
}

export default TransactionModal;

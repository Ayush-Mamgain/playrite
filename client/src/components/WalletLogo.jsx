import React from 'react';

const WalletLogo = ({onHit}) => {
    return (
        <div className='walletLogo' onClick={onHit}>
            WALLET_LOGO
        </div>
    );
}

export default WalletLogo;
